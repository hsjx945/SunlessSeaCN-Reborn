# 90 主代理收口：玩家可见英文清零

- `baseline_id`: `afcc8900eb385b1cef1ec5c3398e7d1cf588931a`
- `phase/attempt`: `ACCEPTANCE_CYCLE_2 / FREEZE_AND_STOP`
- `agent/model/effort`: `root coordinator + luna_deep xhigh + verifier xhigh`
- `hypothesis_or_review_scope`: 清零 macOS 玩家可见的事件文本、弹窗、启动画面和主菜单英文，以真实 Steam 游戏 UI 为最终 oracle。
- `artifact_delta`: 已实现 229 条事件 overlay、可见英文 scanner、弹窗与 UI 补翻、Intro/TitleScreen 双入口注入及对应回归测试；已构建并安装到真实游戏，但未通过最终 UI 验收。
- `validation/failure_class`: 隔离的可见英文、静态 UI 和安装包测试全部 PASS；真实 UI 仍有标题、版本、支持与版权英文。日志确认 `ApplyTitleImage` 用 `580x220` rect 创建 `561x110` 纹理的 sprite，抛出 `ArgumentException`，导致后续标题和底部文本改写均未执行。启动引语仍未实机观察到中文。
- `counters`: `effective_solution_failure=0, delivery_failure=2, transport_retry=0, tool_timeout=7, acceptance_cycle=2`
- `next_transition`: 同一 baseline 已用完两个验收周期，不再返修或开第三轮。后续须以新 baseline 修复 sprite rect/pivot 和启动引语时序，并重新走真实 UI 验收。

## 已确认的有效部分

- 安装到真实游戏的 `Sunless.Game.dll` SHA-256 为 `4ecc41ee6112fb9fc350a1662550fc843662861c61d6a9d80a182e0dad32bf6d`。
- `Player.log` 记录 `content probe 143942: 一名熟练的船员` 和 `macOS static bootstrap initialized; Harmony patches applied.`，证明汉化数据与静态 loader 已加载。
- 真实 Unity 6 addon 的 `events.json` 通过 scanner：`visible-english-findings=0`。
- 旧存档 SHA-256 仍为 `5700a5888e4327bc1bd4e3df0acb23ce4aac751e5b3dae581a7bd06b18a4b181`。
- 真实 app 通过 `codesign --verify --deep --strict`。
- 本轮包为 `dist/SunlessSeaCN-macOS-v6.0.5.zip`，SHA-256 `029ed33a9db6b44a2412bc91bca6a534b35ed0fed35d122ec880c41b29a2fcc3`。

## 未通过的真实界面

- 主标题仍为 `SUNLESS SEA / ZUBMARINER`。
- 底部仍有 `Sunless Sea: Zubmariner V2.3.0.22`、`SUPPORT`、URL 和英文版权文本。
- 启动引语没有通过中文实机验收。
- 自动化输入未能安全进入高分辨率 Warning 和新游戏开局剧情，这两个界面仍未观察。

## Git 与交付状态

- 当前分支：`codex/macos-localization-fix`；输入 commit：`afcc8900eb385b1cef1ec5c3398e7d1cf588931a`。
- 因最终验收失败，本轮改动保留在工作区，**未 commit、未 push、未发布**。
- 真实游戏当前已安装该未通过验收的测试包；剧情数据可加载中文，但主菜单仍有英文。游戏已正常退出。

## 路由评测与沉淀

- `luna_deep xhigh` 适合多文件 overlay/scanner 和静态补丁，但第一次实机修复没有正确处理原 sprite rect 大于替换纹理的情况，需要返工，不是一次完成。
- `verifier xhigh` 必要且有效；静态测试全绿时，真实 UI 捕获到了可重现的 Unity sprite 尺寸异常。
- 本轮经验已落入项目 scanner、测试和交接记录；因问题尚未修复验证，不晋升为新的全局规则或 skill。
