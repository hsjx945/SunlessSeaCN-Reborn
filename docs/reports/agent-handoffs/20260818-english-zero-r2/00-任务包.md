# macOS 可见英文清零 R2 任务包

- `baseline_id`: `b6e0ac5c687b46c554e285778328e299bfbfa62b53a3ec8f3b25141cacc7e694`
- 输入分支：`codex/macos-localization-fix`
- 输入 commit：`afcc8900eb385b1cef1ec5c3398e7d1cf588931a`
- 输入工作区：上一 baseline 未提交的汉化 overlay/scanner/UI 补丁，禁止回退或覆盖。

## 目标

1. 修复 `SS_TitleScreenInit.ApplyTitleImage` 的 sprite rect/pivot/border 逻辑，不得再使用超出替换纹理尺寸的原 rect。
2. 标题图片替换失败不得阻断版本、支持和版权文本的中文改写；各子项独立容错并记录明确日志。
3. 核对真实主菜单对象名/文本，不得只假定 `Version`/`SupportLink`/`Copyright` 一定存在；可以对 TitleScreen 层级做精确的文本内容识别，但不得改动链接点击目标。
4. 修复启动 `EAWarning` 中文图片替换；根据 `IntroScript.PlayEAWarning` 真实时序和对象层级实现，不仅依赖方法入口的单次 `GameObject.Find`。
5. 增加能抓住上述尺寸异常和容错路径的聚焦测试，重新构建 macOS v6.0.5 包。

## 非目标

- 不改 Windows 包、翻译数据内容或安装架构。
- 不发布 release/tag，不提交或 push。
- 不启动真实游戏；真实 UI 由后续 verifier 独立验收。

## 允许修改

- `macos-static/Bootstrap.cs`
- `macos-static/src/SS_TitleScreenInit.cs`
- 与 Intro/EAWarning 直接相关的 `macos-static/src/*.cs`
- `tests/test_macos_static_ui.sh`
- 必要时的构建/包 hash 门禁文件
- `docs/reports/agent-handoffs/20260818-english-zero-r2/01-执行.md`

## 验收

```bash
/bin/bash tests/test_macos_visible_english.sh
/bin/bash tests/test_macos_static_ui.sh
/bin/bash tests/test_macos_package.sh
python3 -m py_compile scripts/apply-macos-localization-overrides.py scripts/audit-macos-visible-english.py
git diff --check
```

完成标准：新包构建成功，上述测试全通过，源码中不再可能用超过 texture 边界的 rect 创建 sprite，标题图失败不阻断底部文本，启动引语有明确的延后/后置替换路径。

## 返回格式

必须返回八字段：`baseline_id`、`phase/attempt`、`agent/model/effort`、`hypothesis_or_review_scope`、`artifact_delta`、`validation/failure_class`、`counters`、`next_transition`；附修改文件、命令证据、新包 SHA-256、风险与经验候选。
