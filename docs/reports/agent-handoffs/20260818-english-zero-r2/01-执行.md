# R2 实现执行交接

- `baseline_id`: `b6e0ac5c687b46c554e285778328e299bfbfa62b53a3ec8f3b25141cacc7e694`
- `phase/attempt`: `implementation/R2-A1`
- `agent/model/effort`: `fix_english_zero_r2 / current runtime / focused build + tests`
- `hypothesis_or_review_scope`: 上一轮实机日志已证明 `Title.png` 为 561x110，而游戏标题 Sprite 使用 580x220 的原 rect；Unity 的 `Sprite.Create` 因 rect 越界抛异常，导致标题图片之后的版本、支持和版权文本全部跳过。EAWarning 也在 `IntroScript.PlayEAWarning` 入口时尚未完成层级创建，单次 `GameObject.Find` 不足。R2 只修复这两个运行时路径及对应测试，不改翻译数据或安装架构。
- `artifact_delta`:
  - `macos-static/src/SS_TitleScreenInit.cs`: 标题替换 Sprite 改用替换纹理自身完整 rect，按源 Sprite 归一化 pivot、缩放并裁剪 border；每个图片子项独立 try/catch；文本改为扫描有效场景中的 `Text`，按版本号、支持链接/URL、版权内容识别，不依赖 `Version`/`SupportLink`/`Copyright` 对象名；图片失败时仍执行文本改写并记录计数日志。
  - `macos-static/src/SS_Utility.cs`: EAWarning 共用 Sprite helper 同样使用新纹理完整 rect，避免复发越界。
  - `macos-static/Bootstrap.cs`: `TryApplyEAWarning` 支持直接对象和有效场景层级扫描；新增 `EAWarningWatcher`，在 `PlayEAWarning` 创建层级后每两帧检查，最多 600 帧，成功或超时自毁并记录日志。
  - `tests/test_macos_static_ui.sh`: 增加完整纹理 rect、禁止原 rect、动态文本扫描、EAWarning watcher 与共享 helper 的静态回归门禁。
  - `build/macos-static/`、`dist/`、`outputs/`: 用隔离原始 DLL 构建并打包 v6.0.5；未启动真实游戏、未修改真实游戏文件、未提交或推送。
- `validation/failure_class`: 聚焦验证通过；未进行真实 UI 验收（按任务包非目标保留给后续 verifier）。
  - `/bin/bash tests/test_macos_visible_english.sh`: PASS，overlay/ZIP 可见英文扫描为 0。
  - `/bin/bash tests/test_macos_static_ui.sh`: PASS，双注入报告、闭包、幂等、Sprite rect 和新 watcher 门禁通过。
  - `/bin/bash tests/test_macos_package.sh`: PASS，安装/重装/卸载/失败回滚/签名和 hash 门禁通过。
  - `python3 -m py_compile scripts/apply-macos-localization-overrides.py scripts/audit-macos-visible-english.py`: PASS。
  - `git diff --check`、相关脚本 `bash -n`: PASS。
  - `python3 scripts/audit-macos-visible-english.py --zip outputs/SunlessSeaCN-macOS-v6.0.5.zip`: PASS，`visible-english-findings=0`。
  - 新包 SHA-256：`a5c1361257b79164a5e40098869ead61f853352084b8cd679ebbc50d7b7a4354`；大小 `5439638` bytes。
  - 构建报告：`target-intro=...IntroScript::PlayEAWarning()`、`bootstrap-intro-action=injected`、`target-title=...TitleScreenInit::Start()`、`bootstrap-title-action=injected`；patched DLL SHA-256 `4ecc41ee6112fb9fc350a1662550fc843662861c61d6a9d80a182e0dad32bf6d`。
- `counters`: `effective_solution_failure=0, delivery_failure=0, transport_retry=0, tool_timeout=0, acceptance_cycle=0`（R2 实现阶段；上一轮累计交接计数仍以 R2 任务包/主代理账本为准）。
- `next_transition`: 交给主代理核对实际 diff 后进入独立 verifier 的真实 macOS UI 验收；重点捕获启动 EAWarning、主菜单标题/版本/支持/版权和开局剧情。若实机仍有英文，只做一次边界明确返修后按验收周期规则冻结，不在本执行代理继续扩展。

## 风险与经验候选

- 风险：运行时实际标题层级可能使用 TextMeshPro 而非 `UnityEngine.UI.Text`；本轮只覆盖项目已有 Text 路径，需由 verifier 以 `Player.log` 和截图确认。
- 风险：两个标题面板复用 `Title.png` 后潜艇标题的显示比例由原 Image 布局决定；本轮保证不加载英文 `TitleZMNew.png`，实际视觉比例留给 UI 验收。
- 经验候选：症状“替换图片抛越界异常并连带跳过后续文本”；证据“源 Sprite 580x220、替换纹理 561x110、Unity `ArgumentException`”；修复“Sprite.Create 使用替换纹理完整 rect，pivot/border 按比例裁剪，图片子项独立容错”；适用范围“Unity UI Sprite 替换”；反例“需要保留原图裁切区域的非等尺寸资源，需单独定义映射”。
