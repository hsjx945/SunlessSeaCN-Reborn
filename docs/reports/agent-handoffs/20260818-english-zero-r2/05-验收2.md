# ACCEPTANCE_CYCLE_2 实机验收

- `baseline_id`: `b6e0ac5c687b46c554e285778328e299bfbfa62b53a3ec8f3b25141cacc7e694`
- `phase/attempt`: `ACCEPTANCE_CYCLE_2`
- `agent/model/effort`: `root / GPT-5 / 真实 macOS UI + 回归门禁`
- `hypothesis_or_review_scope`: 复核上轮的版本误判 blocker、新版本比较及内容基准修复的影响面，并按用户最终边界将外部商业广告图中的英文排除在游戏内容缺翻验收之外。
- `artifact_delta`: 本验收步骤未修改业务代码；新增本记录。
- `validation/failure_class`:
  - 真实 Steam app 启动后，启动引语、标题、菜单、版本/支持/版权文字均显示中文。
  - 真实主菜单右下角已显示“故事已是最新！”，证明 `2.3.0.22` 不再被服务端 `2.2.4.3141` 误判为旧版。证据：`evidence/real-ui-r2-cycle2-main-menu-updated.jpeg`。
  - `Player-r2-cycle2.log` 显示 EAWarning、标题图和页脚本地化成功，两次使用内置内容基准 `201710261506`；无 SSTranslator 异常或 Sprite 越界。
  - 真实自动存档 SHA-256 保持 `5700a5888e4327bc1bd4e3df0acb23ce4aac751e5b3dae581a7bd06b18a4b181`。
  - `tests/test_macos_visible_english.sh`: PASS。
  - `tests/test_macos_static_ui.sh`: PASS。
  - `tests/test_macos_package.sh`: PASS。
  - Bash 语法、Python compile、`git diff --check`: PASS。
  - ZIP SHA-256: `73d11fcbd3c795557b0e48628e8f008a6f92e9f450d93887c46e5e632ed18650`。
  - 账号管理的 `ReAuthenticate` 显示语句已翻译，而版本结果由全局 `SoftwareVersionRequirement.SoftwareNewEnough` 补丁提供，与已实机通过的主菜单共用比较逻辑。Unity 未接收桌面自动化的合成点击，故该面板本身未获得独立实机截图；不将其记为已目视验收。
  - 用户明确允许外部商业广告图保留英文；该英文不计为游戏内容缺翻。
  - `failure_class`: 无新的代码或包 blocker；账号管理面板的独立实机截图为未获取的产品证据，不重置验收周期。
- `counters`: `effective_solution_failure=0, delivery_failure=1, transport_retry=0, tool_timeout=0, acceptance_cycle=2`
- `next_transition`: 等待同一验收周期的独立 reviewer 核对共用版本路径和新 diff；若无 blocker，可提交并推送分支，但不自动发布 release/tag。

## 经验候选

- 症状：已是最新版本仍显示“需要更新”。
- 原始证据：当前 `2.3.0.22`，服务端要求 `2.2.4.3141`，原算法删除点后比较为 `23022 < 2243141`。
- 已验证根因：版本号被当作拼接整数，且缺失 `GameVersion` 时使用 `DateTime.MinValue`。
- 可复用修复：只对可安全解析的版本做分段比较；只在 key 缺失时用内置内容日期补齐基准；解析失败保留原路径。
- 验证：纯逻辑回归、包往返测试及真实主菜单“故事已是最新！”全部通过。
- 适用范围：当前 macOS Unity 6 BuildID `24437295`；未核验的未来版本需重新比较服务端数据。
