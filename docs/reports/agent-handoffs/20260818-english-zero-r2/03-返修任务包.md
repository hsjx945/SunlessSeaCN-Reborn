# 版本误判局部返修任务包

- `baseline_id`: `b6e0ac5c687b46c554e285778328e299bfbfa62b53a3ec8f3b25141cacc7e694`
- `phase/attempt`: `ACCEPTANCE_CYCLE_1 blocker remediation`

## 唯一目标

1. 为 `Sunless.Game.Import.SoftwareVersionRequirement.SoftwareNewEnough` 实现严格的分段版本比较：`2.3.0.22 > 2.2.4.3141`；支持首字母 `v`、首尾空白和不等长数字段。只在两个版本均能安全解析时跳过原方法；解析失败保留原游戏逻辑并记录日志，不得一律返回 true。
2. 修复 `PlayerPrefs.GameVersion` 缺失时的内容基准：仅在 key 缺失且本地使用随游戏内置内容时，以 `GameProvider.CONTENT_VERSION_NUMBER` 作为比较基准。不得屏蔽真实新内容、真实旧软件或网络错误。
3. 增加机器化回归测试，至少覆盖 `2.3.0.22 > 2.2.4.3141`、真实旧版、平级、非法版本不强制通过，以及缺失 `GameVersion` 时使用内置内容基准。

## 允许修改

- 新增或修改与 `SoftwareVersionRequirement` / `Importer.CheckIfNewContent` 直接相关的 `macos-static/src/*.cs`
- `tests/test_macos_static_ui.sh` 及必要的包 hash 门禁
- `docs/reports/agent-handoffs/20260818-english-zero-r2/04-返修执行.md`

禁止改动翻译数据、标题/启动修复、Windows 包、用户文件，禁止启动游戏、commit、push、release/tag。

## 验收

```bash
/bin/bash tests/test_macos_visible_english.sh
/bin/bash tests/test_macos_static_ui.sh
/bin/bash tests/test_macos_package.sh
git diff --check
```

返回八字段、实际 diff、测试、新包与 DLL SHA-256、风险。
